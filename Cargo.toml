[package]
name = "strategem-hero"
version = "0.3.0"
edition = "2021"

[profile.release]
strip = true
opt-level = "z"   # Optimize for size.
lto = true
codegen-units = 1
panic = "abort"

[dependencies]
crossterm = { version = "0.27" }
chrono = { version = "0.4" }
rand = { version = "0.8" }
